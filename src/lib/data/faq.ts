/** Structural data only — question/answer text lives in the
 * `FAQ.items` translation namespace, keyed by `id`. */
export const FAQ_ITEM_IDS = [
  "howAiWorks",
  "updateFrequency",
  "cancelAnytime",
  "beginners",
  "markets",
  "copyTrading",
] as const;
